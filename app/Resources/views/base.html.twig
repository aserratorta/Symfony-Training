<!DOCTYPE html>
<html>

<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <title>{% block title %}{% endblock %} | Symfony_Training</title>
    {% block stylesheets %}{% endblock %}
</head>

<body id="{% block id '' %}"><div id="contenedor">
    {% block body %}{% endblock %}

    <footer>
        &copy; {{  'now' |date('Y') }}-
        <a href="{{ path('static_page', {'page':'help'}) }}">Ajuda</a>
        <a href="{{ path('static_page', {'page':'contact'}) }}">Contacte</a>
        <a href="{{ path('static_page', {'page':'privacity'}) }}">Privacitat</a>
        <a href="{{ path('static_page', {'page':'aboutus'}) }}">Sobre natros</a>
    </footer>

    {% block javascripts %}{% endblock %}
</div></body>
</html>